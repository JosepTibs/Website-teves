{% extends 'base.html.twig' %}

{% block title %}Dish index{% endblock %}

{% block body %}
    <h1>Dish index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Description</th>
                <th>Category</th>
                <th>Image</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for dish in dishes %}
            <tr>
                <td>{{ dish.id }}</td>
                <td>{{ dish.Name }}</td>
                <td>{{ dish.Description }}</td>
                <td>{{ dish.Category }}</td>
                <td>{{ dish.Image }}</td>
                <td>
                    <a href="{{ path('app_dish_show', {'id': dish.id}) }}">show</a>
                    <a href="{{ path('app_dish_edit', {'id': dish.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_dish_new') }}">Create new</a>
{% endblock %}
